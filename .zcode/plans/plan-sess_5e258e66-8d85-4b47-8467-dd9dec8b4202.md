# Plano de Ação — Implementação Completa do Benchmark Multi-Linguagem

## Resumo Executivo

O projeto tem **11 linguagens** com **~35 frameworks**. Destes, **6 linguagens estão completas** (C#, Rust, Java, Go, Kotlin, Node.js) e **5 precisam de trabalho** (Python, Bun, Deno, Dart, GraalVM). Além disso, há correções de infraestrutura (scripts de deploy/benchmark).

---

## Fase 1: Correções Críticas de Infraestrutura (Prioridade ALTA)

### 1.1 — Fix `scripts/deploy-k8s.sh` e `scripts/undeploy-k8s.sh`
- Ambos estão hardcoded para C#. Tornar genéricos aceitando nome da linguagem como parâmetro
- Adicionar detecção automática do path dos manifests K8s baseado na linguagem

### 1.2 — Criar `scripts/benchmark-wrk-csharp.sh`
- É o único script de benchmark por linguagem que não existe
- Seguir o padrão dos scripts mais recentes (python, bun, deno, dart, graalvm)

---

## Fase 2: Python (3 frameworks) — Prioridade ALTA

### 2.1 — FastAPI ✅ COMPLETO (referência)
- Nenhuma ação necessária

### 2.2 — Django (90% → 100%)
**Arquivos para corrigir:**
- `src/python/django/benchmark/views.py` vs `benchmark/views/` — resolver conflito (remover views.py flat, manter package)
- `src/python/django/k8s/deployment.yaml` — corrigir YAML malformado na linha 72-73
- `src/python/django/Dockerfile` — adicionar multi-stage build, non-root user, HEALTHCHECK
- `src/python/django/build.sh` — adicionar targets `test`, `check`, `fmt`, `docker-push`
- `src/python/django/benchmark/settings.py` — ler DATABASE_URL de env vars (não hardcoded)
- `src/python/django/benchmark/services/cache_service.py` — ler REDIS_URL de env vars
- Criar `src/python/django/run.sh`
- Criar `src/python/django/README.md`

### 2.3 — Flask (85% → 100%)
**Arquivos para corrigir:**
- `src/python/flask/app/routes/__init__.py` — adicionar exports
- `src/python/flask/app/routes/*.py` — corrigir uso de `asyncio.run()` dentro de rotas sync (usar biblioteca sync do psycopg2/redis ou fazer async de verdade)
- `src/python/flask/build.sh` — adicionar targets `test`, `check`, `fmt`, `docker-push`
- `src/python/flask/Dockerfile` — melhorar (multi-stage, non-root, HEALTHCHECK)
- Criar `src/python/flask/README.md`

---

## Fase 3: Bun (3 frameworks) — Prioridade MÉDIA

### 3.1 — Elysia ✅ COMPLETO
### 3.2 — Hono ✅ COMPLETO (falta README)
### 3.3 — Bun.serve ✅ COMPLETO (falta README)

**Ações:**
- Criar `src/bun/hono/README.md`
- Criar `src/bun/bun_serve/README.md`

---

## Fase 4: Deno (4 frameworks) — Prioridade ALTA

### 4.1 — Oak ✅ COMPLETO (referência)

### 4.2 — Fresh (40% → 100%) — REESCREVER
**Problemas críticos:**
- `server.ts` usa `Application.get()` que não existe no Fresh 1.x
- Dockerfile só copia `server.ts` mas imports apontam para `./src/services/`
- Falta `deno.json` com import map
- Falta rotas `json.ts`, `database.ts`, `cache.ts` em `routes/api/`

**Ações:**
- Reescrever `src/deno/fresh/deno.json` com import map completo
- Reescriar `src/deno/fresh/server.ts` — usar Fresh corretamente com islands/routes
- Criar `routes/api/json.ts`, `routes/api/database.ts`, `routes/api/cache.ts`
- Corrigir `src/deno/fresh/Dockerfile` para copiar todos os arquivos necessários
- Corrigir `src/deno/fresh/src/services/` — usar imports do deno.json
- Criar `src/deno/fresh/README.md`

### 4.3 — Hono (40% → 100%) — REESCREVER
**Problemas críticos:**
- Falta `deno.json` com import map
- Dockerfile só copia `server.ts`
- Services são cópia do Fresh (podem estar errados)

**Ações:**
- Criar `src/deno/hono/deno.json` com import map (hono, postgres, redis)
- Reescrever `src/deno/hono/server.ts` — usar Hono for Deno corretamente
- Criar `src/deno/hono/deps.ts` com exports centralizados
- Corrigir `src/deno/hono/Dockerfile` — copiar todos os arquivos
- Corrigir services para usar imports do deno.json
- Criar `src/deno/hono/run.sh`
- Criar `src/deno/hono/README.md`

### 4.4 — Deno.serve (40% → 100%) — REESCREVER
**Problemas críticos:**
- Mesmos problemas do Hono (falta deno.json, Dockerfile incompleto)

**Ações:**
- Criar `src/deno/deno_serve/deno.json` com import map
- Reescrever `src/deno/deno_serve/server.ts` — usar `Deno.serve()` nativo
- Criar `src/deno/deno_serve/deps.ts`
- Corrigir Dockerfile
- Corrigir services
- Criar `src/deno/deno_serve/run.sh`
- Criar `src/deno/deno_serve/README.md`

---

## Fase 5: Dart (1 framework) — Prioridade MÉDIA

### 5.1 — Vaden (70% → 100%)
**Problemas:**
- Falta `bin/server.dart` (entrypoint que Dockerfile referencia)
- Falta arquivos `.g.dart` gerados (json_annotation)
- Framework `vaden` pode não existir no pub.dev

**Ações:**
- Verificar se `vaden` existe no pub.dev; se não, trocar para `shelf` ou `dart_frog` (frameworks Dart reais)
- Criar `bin/server.dart` como entrypoint
- Adicionar `build_runner` ao pubspec.yaml e gerar `.g.dart`
- Corrigir imports faltantes (`dart:io` para `Platform`)
- Corrigir Dockerfile para incluir `dart pub run build_runner build`
- Criar README.md se não existir

---

## Fase 6: GraalVM (6 frameworks) — Prioridade ALTA

### 6.1 — Vert.x (95% → 100%)
**Correções:**
- `src/graalvm/vertx/src/main/java/com/benchmark/vertx/handlers/HealthHandler.java` — corrigir erro de sintaxe (falta `class HealthHandler implements`)
- `src/graalvm/vertx/src/main/java/com/benchmark/vertx/server/VertxServer.java` — resolver conflito de import `CorsHandler`

### 6.2 — Spring Boot no GraalVM (90% → 100%)
- `src/graalvm/spring/Dockerfile` — adicionar non-root user, HEALTHCHECK, multi-stage
- Criar `src/graalvm/spring/run.sh`
- Criar `src/graalvm/spring/README.md`

### 6.3 — gSpring (90% → 100%)
- `src/graalvm/gspring/Dockerfile` — adicionar non-root user, HEALTHCHECK
- Criar `src/graalvm/gspring/run.sh`
- Criar `src/graalvm/gspring/README.md`

### 6.4 — Micronaut (90% → 100%)
- `src/graalvm/micronaut/Dockerfile` — adicionar non-root user, HEALTHCHECK
- Criar `src/graalvm/micronaut/run.sh`
- Criar `src/graalvm/micronaut/README.md`

### 6.5 — gMicronaut (90% → 100%)
- `src/graalvm/gmicronaut/Dockerfile` — adicionar non-root user, HEALTHCHECK
- Criar `src/graalvm/gmicronaut/run.sh`
- Criar `src/graalvm/gmicronaut/README.md`

### 6.6 — Helidon (80% → 100%)
- `src/graalvm/helidon/src/main/java/com/benchmark/Main.java` — remover credenciais hardcoded, ler de env vars
- `src/graalvm/helidon/Dockerfile` — adicionar non-root user, HEALTHCHECK, multi-stage
- Criar `src/graalvm/helidon/run.sh`
- Criar `src/graalvm/helidon/README.md`

---

## Fase 7: Frameworks Alternativos (baixa prioridade — apenas se solicitado)

Estes frameworks extras existem no projeto mas não são prioridade do Makefile principal:
- Go: Gin, Echo, Chi
- Java: Spring Boot, Micronaut
- Kotlin: Spring Boot, http4k
- Node.js: Express, NestJS
- Python: Django, Flask (já cobertos acima)

Apenas garantir que compila e tem Dockerfile funcional.

---

## Fase 8: Validação Final

### 8.1 — Para cada implementação:
1. `build.sh local` — compilar sem erros
2. `build.sh docker` — Docker build sem erros
3. Validar K8s manifests com `kubectl apply --dry-run=client`
4. Health check manual via port-forward

### 8.2 — Atualizar documentação:
- `FINAL_SUMMARY.md` — marcar todas as 11 linguagens como completas
- `IMPLEMENTACAO_STATUS.md` — atualizar progresso para 100%
- Criar `BENCHMARK_RESULTS.md` com resultados reais (após execução)

### 8.3 — Executar benchmark completo:
- `make deploy-all` → `make benchmark-all` → `make undeploy-all`

---

## Ordem de Execução Recomendada

```
Fase 1 (Infra)     → ~30 min  │ Corrigir scripts de deploy/benchmark
Fase 2 (Python)    → ~2 horas │ Django + Flask (já quase prontos)
Fase 3 (Bun)       → ~15 min  │ Apenas READMEs
Fase 4 (Deno)      → ~3 horas │ Fresh + Hono + Deno.serve (reescrita)
Fase 5 (Dart)      → ~2 horas │ Vaden (verificar framework, criar entrypoint)
Fase 6 (GraalVM)   → ~2 horas │ 6 frameworks com correções pontuais
Fase 7 (Extras)    → ~1 hora  │ Frameworks alternativos (se solicitado)
Fase 8 (Validação) → ~1 hora  │ Build + Docker + K8s dry-run
```

**Total estimado: ~12 horas de trabalho**

---

## Priorização por Impacto

| Prioridade | Linguagem | Impacto |
|------------|-----------|---------|
| 🔴 Alta | Deno (Fresh/Hono/deno_serve) | 3 frameworks a 40% — maior trabalho |
| 🔴 Alta | Python (Django/Flask) | Quase prontos, rápido de fechar |
| 🔴 Alta | GraalVM (6 frameworks) | Correções pontuais mas muitos arquivos |
| 🟡 Média | Dart (Vaden) | Framework incerto, precisa investigar |
| 🟢 Baixa | Bun | Apenas READMEs faltando |
| 🟢 Baixa | Infraestrutura | Scripts de deploy/benchmark |
