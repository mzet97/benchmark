apiVersion: v1
kind: Service
metadata:
  name: graphql-dart-leto
  labels:
    app: graphql-dart-leto
    benchmark: "true"
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: graphql-dart-leto
