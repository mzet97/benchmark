apiVersion: apps/v1
kind: Deployment
metadata:
  name: graphql-server2
  labels:
    app: graphql-server2
spec:
  replicas: 1
  selector:
    matchLabels:
      app: graphql-server2
  template:
    metadata:
      labels:
        app: graphql-server2
    spec:
      containers:
        - name: graphql-server2
          image: graphql-server2:latest
          ports:
            - containerPort: 3000
          envFrom:
            - configMapRef:
                name: graphql-server2-config
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          readinessProbe:
            httpGet:
              path: /health
              port: 3000
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 3000
            initialDelaySeconds: 10
            periodSeconds: 30
