name: benchmark_dart_vaden
description: REST API Benchmark - Dart + Vaden (Shelf) Framework
version: 1.0.0
publish_to: 'none'

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  shelf: ^1.4.0
  shelf_router: ^1.1.0
  postgres: ^3.0.0
  redis: ^4.0.0
  logging: ^1.2.0

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
