// Routes are defined directly in bin/server.dart using shelf_router.
// This barrel file is kept for project structure.
//
// All 5 benchmark endpoints are implemented:
// - GET /health
// - GET /healthz
// - GET /readyz
// - GET /json
// - GET /db/simple?id={id}
// - GET /db/complex?days={days}
// - GET /cache?key={key}
