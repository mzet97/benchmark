name: benchmark_grpc_dart
description: gRPC benchmark server implementation in Dart
version: 1.0.0

environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  grpc: ^4.0.1
  protobuf: ^3.1.0
  postgres: ^3.0.7
  redis: ^3.1.0

dev_dependencies:
  lints: ^4.0.0
