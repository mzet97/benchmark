apiVersion: v1
kind: Service
metadata:
  name: graalvm-spring
  labels:
    app: graalvm-spring
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: graalvm-spring
---
apiVersion: v1
kind: Service
metadata:
  name: graalvm-spring-headless
  labels:
    app: graalvm-spring
spec:
  type: ClusterIP
  clusterIP: None
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: graalvm-spring
