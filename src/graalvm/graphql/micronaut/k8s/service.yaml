apiVersion: v1
kind: Service
metadata:
  name: micronaut-graphql-native
  labels:
    app: micronaut-graphql-native
    benchmark: "true"
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: micronaut-graphql-native
