apiVersion: v1
kind: Service
metadata:
  name: spring-graphql-native
  labels:
    app: spring-graphql-native
    benchmark: "true"
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: spring-graphql-native
