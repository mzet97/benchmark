apiVersion: v1
kind: Service
metadata:
  name: smallrye-graphql
  labels:
    app: smallrye-graphql
    benchmark: "true"
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: smallrye-graphql
