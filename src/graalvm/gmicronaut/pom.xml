<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>io.micronaut.platform</groupId>
        <artifactId>micronaut-platform</artifactId>
        <version>4.5.0</version>
    </parent>

    <groupId>com.benchmark</groupId>
    <artifactId>graalvm-micronaut</artifactId>
    <version>1.0.0</version>

    <properties>
        <maven.compiler.release>21</maven.compiler.release>
    </properties>

    <dependencies>
        <dependency>
            <groupId>io.micronaut</groupId>
            <artifactId>micronaut-http-server-netty</artifactId>
        </dependency>
        <dependency>
            <groupId>io.micronaut</groupId>
            <artifactId>micronaut-runtime</artifactId>
        </dependency>
        <dependency>
            <groupId>io.micronaut</groupId>
            <artifactId>micronaut-jackson-databind</artifactId>
        </dependency>
        <dependency>
            <groupId>io.micronaut.sql</groupId>
            <artifactId>micronaut-jdbc-hikari</artifactId>
        </dependency>
        <!-- micronaut-data-jdbc deliberately NOT included. The /db endpoints
             use raw JDBC in DatabaseService (User/UserStats are plain Jackson
             DTOs, not @MappedEntity, so no @JdbcRepository can be generated).
             micronaut-data-jdbc wraps every DataSource in a transactional
             proxy, which makes DatabaseService.getConnection() throw
             "No current connection present. Consider declaring @Transactional"
             even though no Micronaut Data repository exists. micronaut-jdbc-
             hikari alone produces the plain HikariDataSource the queries need. -->
        <dependency>
            <groupId>io.micronaut.redis</groupId>
            <artifactId>micronaut-redis-lettuce</artifactId>
        </dependency>
        <dependency>
            <groupId>org.postgresql</groupId>
            <artifactId>postgresql</artifactId>
            <scope>runtime</scope>
        </dependency>
        <dependency>
            <groupId>ch.qos.logback</groupId>
            <artifactId>logback-classic</artifactId>
        </dependency>
    </dependencies>

  <build>
    <plugins>
      <!-- Explicitly declare the compiler plugin so micronaut-inject-java is
           guaranteed to run. It generates the bean/route introspection metadata
           that lets the runtime discover @Controller, @Singleton and @Inject
           beans. (micronaut-data-processor used to live here too, but with
           micronaut-data-jdbc removed, see dependencies above, there is no
           @JdbcRepository left to implement.) -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <configuration>
          <annotationProcessorPaths combine.children="append">
            <path>
              <groupId>io.micronaut</groupId>
              <artifactId>micronaut-inject-java</artifactId>
              <version>${micronaut.core.version}</version>
            </path>
            <!-- micronaut-data-processor removed: with micronaut-data-jdbc gone
                 there is no @Repository/@JdbcRepository to implement, and the
                 processor's DataSource transaction wrapping is what broke the
                 raw-JDBC getConnection() calls in DatabaseService. -->
          </annotationProcessorPaths>
        </configuration>
      </plugin>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-dependency-plugin</artifactId>
        <version>3.6.1</version>
        <executions>
          <execution>
            <id>copy-deps</id>
            <phase>package</phase>
            <goals><goal>copy-dependencies</goal></goals>
            <configuration>
              <outputDirectory>${project.build.directory}/dependency</outputDirectory>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
</project>
