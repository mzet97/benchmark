apiVersion: v1
kind: Service
metadata:
  name: rust-actix-web
  namespace: benchmark
  labels:
    app: rust-actix-web
    tier: api
spec:
  type: ClusterIP
  selector:
    app: rust-actix-web
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  sessionAffinity: None
