apiVersion: v1
kind: Service
metadata:
  name: graphql-rust-juniper
  labels:
    app: graphql-rust-juniper
spec:
  selector:
    app: graphql-rust-juniper
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
