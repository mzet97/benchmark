apiVersion: v1
kind: Service
metadata:
  name: graphql-rust
  labels:
    app: graphql-rust
spec:
  selector:
    app: graphql-rust
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
