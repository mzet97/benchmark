apiVersion: apps/v1
kind: Deployment
metadata:
  name: graphql-rust-actix
  labels:
    app: graphql-rust-actix
spec:
  replicas: 1
  selector:
    matchLabels:
      app: graphql-rust-actix
  template:
    metadata:
      labels:
        app: graphql-rust-actix
    spec:
      containers:
        - name: server
          image: graphql-rust-async-graphql-actix:latest
          ports:
            - containerPort: 8080
          envFrom:
            - configMapRef:
                name: graphql-rust-actix-config
          resources:
            requests:
              cpu: 100m
              memory: 64Mi
            limits:
              cpu: 500m
              memory: 256Mi
          readinessProbe:
            httpGet:
              path: /graphql
              port: 8080
            initialDelaySeconds: 2
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /graphql
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
