apiVersion: v1
kind: Service
metadata:
  name: benchmark-grpc-grpcio
  labels:
    app: benchmark-grpc-grpcio
spec:
  type: ClusterIP
  ports:
    - port: 50051
      targetPort: 50051
      protocol: TCP
      name: grpc
  selector:
    app: benchmark-grpc-grpcio
