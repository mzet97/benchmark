#!/usr/bin/env bash
set -euo pipefail

SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
PROJECT_ROOT="$(cd "$SCRIPT_DIR/../../.." && pwd)"
IMAGE_NAME="benchmark-grpc-volo"
IMAGE_TAG="${1:-latest}"

echo "=== Building Rust Volo gRPC server ==="

echo "Proto code will be generated by volo-build during cargo build"

echo "Building Docker image: $IMAGE_NAME:$IMAGE_TAG"
docker build \
  -f "$SCRIPT_DIR/Dockerfile" \
  -t "$IMAGE_NAME:$IMAGE_TAG" \
  "$PROJECT_ROOT"

echo "=== Build complete: $IMAGE_NAME:$IMAGE_TAG ==="
