apiVersion: v1
kind: Service
metadata:
  name: graphql-python-graphene
  labels:
    app: graphql-python-graphene
    benchmark: "true"
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: graphql-python-graphene
