apiVersion: apps/v1
kind: Deployment
metadata:
  name: graphql-python-graphene
  labels:
    app: graphql-python-graphene
    benchmark: "true"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: graphql-python-graphene
  template:
    metadata:
      labels:
        app: graphql-python-graphene
        benchmark: "true"
    spec:
      containers:
        - name: server
          image: graphql-python-graphene:latest
          ports:
            - containerPort: 8080
              name: http
          envFrom:
            - configMapRef:
                name: graphql-python-graphene-config
            - secretRef:
                name: benchmark-secrets
          resources:
            requests:
              cpu: 100m
              memory: 64Mi
            limits:
              cpu: 500m
              memory: 256Mi
          readinessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /health
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 10
