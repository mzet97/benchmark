apiVersion: v1
kind: Service
metadata:
  name: graphql-python
  labels:
    app: graphql-python
spec:
  selector:
    app: graphql-python
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
