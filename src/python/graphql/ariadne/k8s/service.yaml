apiVersion: v1
kind: Service
metadata:
  name: graphql-python-ariadne
  labels:
    app: graphql-python-ariadne
    benchmark: "true"
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: graphql-python-ariadne
