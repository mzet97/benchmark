apiVersion: v1
kind: Service
metadata:
  name: python-grpc-grpcio
  labels:
    app: python-grpc-grpcio
    component: benchmark
spec:
  type: ClusterIP
  selector:
    app: python-grpc-grpcio
  ports:
    - port: 50051
      targetPort: 50051
      protocol: TCP
      name: grpc
