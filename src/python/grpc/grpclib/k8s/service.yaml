apiVersion: v1
kind: Service
metadata:
  name: python-grpc-grpclib
  labels:
    app: python-grpc-grpclib
    component: benchmark
spec:
  type: ClusterIP
  selector:
    app: python-grpc-grpclib
  ports:
    - port: 50051
      targetPort: 50051
      protocol: TCP
      name: grpc
