apiVersion: v1
kind: Service
metadata:
  name: bun-graphql-hono
  labels:
    app: bun-graphql-hono
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: bun-graphql-hono
---
apiVersion: v1
kind: Service
metadata:
  name: bun-graphql-hono-headless
  labels:
    app: bun-graphql-hono
spec:
  type: ClusterIP
  clusterIP: None
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: bun-graphql-hono
