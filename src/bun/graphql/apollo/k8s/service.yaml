apiVersion: v1
kind: Service
metadata:
  name: bun-graphql-apollo
  labels:
    app: bun-graphql-apollo
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: bun-graphql-apollo
---
apiVersion: v1
kind: Service
metadata:
  name: bun-graphql-apollo-headless
  labels:
    app: bun-graphql-apollo
spec:
  type: ClusterIP
  clusterIP: None
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: bun-graphql-apollo
