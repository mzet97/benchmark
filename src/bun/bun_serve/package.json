{
  "name": "benchmark-bun-serve",
  "version": "1.0.0",
  "description": "High-performance REST API benchmark using Bun runtime with native Bun.serve",
  "type": "module",
  "main": "src/server.ts",
  "scripts": {
    "dev": "bun run --watch src/server.ts",
    "start": "bun src/server.ts",
    "build": "tsc",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "ioredis": "^5.3.2",
    "pg": "^8.11.3",
    "pino": "^8.17.0",
    "pino-pretty": "^11.0.0"
  },
  "devDependencies": {
    "@types/pg": "^8.10.9",
    "@types/bun": "^1.0.0",
    "typescript": "^5.3.3"
  }
}
