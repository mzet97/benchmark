apiVersion: apps/v1
kind: Deployment
metadata:
  name: benchmark-bun-nice-grpc
  labels:
    app: benchmark-bun-nice-grpc
    runtime: bun
    framework: nice-grpc
spec:
  replicas: 1
  selector:
    matchLabels:
      app: benchmark-bun-nice-grpc
  template:
    metadata:
      labels:
        app: benchmark-bun-nice-grpc
        runtime: bun
        framework: nice-grpc
    spec:
      containers:
        - name: server
          image: benchmark-bun-nice-grpc:latest
          ports:
            - containerPort: 50051
              name: grpc
              protocol: TCP
          envFrom:
            - configMapRef:
                name: benchmark-config
            - secretRef:
                name: benchmark-secrets
          resources:
            requests:
              cpu: "250m"
              memory: "256Mi"
            limits:
              cpu: "1000m"
              memory: "512Mi"
          readinessProbe:
            tcpSocket:
              port: 50051
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            tcpSocket:
              port: 50051
            initialDelaySeconds: 10
            periodSeconds: 30
