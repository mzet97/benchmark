apiVersion: v1
kind: Service
metadata:
  name: benchmark-bun-grpc-js
  labels:
    app: benchmark-bun-grpc-js
spec:
  type: ClusterIP
  ports:
    - port: 50051
      targetPort: 50051
      protocol: TCP
      name: grpc
  selector:
    app: benchmark-bun-grpc-js
