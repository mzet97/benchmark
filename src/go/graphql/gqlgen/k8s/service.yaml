apiVersion: v1
kind: Service
metadata:
  name: graphql-go
  labels:
    app: graphql-go
spec:
  selector:
    app: graphql-go
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
