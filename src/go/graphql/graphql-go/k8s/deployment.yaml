apiVersion: apps/v1
kind: Deployment
metadata:
  name: graphql-go-graphql
  labels:
    app: graphql-go-graphql
spec:
  replicas: 1
  selector:
    matchLabels:
      app: graphql-go-graphql
  template:
    metadata:
      labels:
        app: graphql-go-graphql
    spec:
      containers:
        - name: server
          image: graphql-go-graphql:latest
          ports:
            - containerPort: 8080
          envFrom:
            - configMapRef:
                name: graphql-go-graphql-config
          resources:
            requests:
              cpu: 100m
              memory: 64Mi
            limits:
              cpu: 500m
              memory: 256Mi
          readinessProbe:
            httpGet:
              path: /graphql
              port: 8080
            initialDelaySeconds: 2
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /graphql
              port: 8080
            initialDelaySeconds: 5
            periodSeconds: 10
