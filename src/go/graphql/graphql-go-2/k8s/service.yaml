apiVersion: v1
kind: Service
metadata:
  name: graphql-graphql-go
  labels:
    app: graphql-graphql-go
spec:
  selector:
    app: graphql-graphql-go
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
  type: ClusterIP
