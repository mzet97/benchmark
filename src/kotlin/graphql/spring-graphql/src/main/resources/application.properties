# Server
server.port=3000

# GraphQL
spring.graphql.path=/graphql
spring.graphql.graphiql.enabled=false
spring.graphql.introspection.disabled=true

# PostgreSQL
spring.datasource.url=${DATABASE_URL:postgresql://app:Admin@123@spsql.home.arpa:5432/benchmark_api}
spring.datasource.driver-class-name=org.postgresql.Driver

# Redis
spring.data.redis.url=${REDIS_URL:redis://:Admin@123@redis.home.arpa:30379}

# Logging
logging.level.root=WARN
logging.level.benchmark=INFO
