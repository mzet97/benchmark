apiVersion: v1
kind: Service
metadata:
  name: benchmark-grpc-kotlin-spring
  labels:
    app: benchmark-grpc-kotlin-spring
    component: grpc-server
    benchmark: "true"
spec:
  type: ClusterIP
  selector:
    app: benchmark-grpc-kotlin-spring
  ports:
    - port: 50051
      targetPort: 50051
      protocol: TCP
      name: grpc
