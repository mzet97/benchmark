apiVersion: v1
kind: Service
metadata:
  name: benchmark-grpc-kotlin-armeria
  labels:
    app: benchmark-grpc-kotlin-armeria
    component: grpc-server
    benchmark: "true"
spec:
  type: ClusterIP
  selector:
    app: benchmark-grpc-kotlin-armeria
  ports:
    - port: 50051
      targetPort: 50051
      protocol: TCP
      name: grpc
