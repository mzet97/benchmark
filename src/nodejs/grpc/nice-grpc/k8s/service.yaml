apiVersion: v1
kind: Service
metadata:
  name: benchmark-nice-grpc
  labels:
    app: benchmark-nice-grpc
    component: grpc-server
    benchmark: "true"
spec:
  type: ClusterIP
  selector:
    app: benchmark-nice-grpc
  ports:
    - port: 50051
      targetPort: 50051
      protocol: TCP
      name: grpc
