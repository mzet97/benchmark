apiVersion: apps/v1
kind: Deployment
metadata:
  name: benchmark-connectrpc
  labels:
    app: benchmark-connectrpc
    component: grpc-server
    benchmark: "true"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: benchmark-connectrpc
  template:
    metadata:
      labels:
        app: benchmark-connectrpc
        component: grpc-server
        benchmark: "true"
    spec:
      containers:
        - name: connectrpc
          image: benchmark/connectrpc:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 50051
              name: grpc
              protocol: TCP
          envFrom:
            - configMapRef:
                name: benchmark-connectrpc-config
            - secretRef:
                name: benchmark-secrets
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "256Mi"
          livenessProbe:
            httpGet:
              path: /healthz
              port: 50051
            initialDelaySeconds: 15
            periodSeconds: 20
          readinessProbe:
            httpGet:
              path: /healthz
              port: 50051
            initialDelaySeconds: 5
            periodSeconds: 10
      restartPolicy: Always
      terminationGracePeriodSeconds: 30
