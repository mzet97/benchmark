apiVersion: v1
kind: Service
metadata:
  name: benchmark-connectrpc
  labels:
    app: benchmark-connectrpc
    component: grpc-server
    benchmark: "true"
spec:
  type: ClusterIP
  selector:
    app: benchmark-connectrpc
  ports:
    - port: 50051
      targetPort: 50051
      protocol: TCP
      name: grpc
