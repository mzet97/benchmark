apiVersion: apps/v1
kind: Deployment
metadata:
  name: graphql-yoga
  labels:
    app: graphql-yoga
spec:
  replicas: 1
  selector:
    matchLabels:
      app: graphql-yoga
  template:
    metadata:
      labels:
        app: graphql-yoga
    spec:
      containers:
        - name: graphql-yoga
          image: graphql-yoga:latest
          ports:
            - containerPort: 3000
          envFrom:
            - configMapRef:
                name: graphql-yoga-config
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          readinessProbe:
            httpGet:
              path: /graphql
              port: 3000
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /graphql
              port: 3000
            initialDelaySeconds: 10
            periodSeconds: 30
