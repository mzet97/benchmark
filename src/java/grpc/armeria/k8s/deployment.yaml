apiVersion: apps/v1
kind: Deployment
metadata:
  name: benchmark-grpc-java-armeria
  labels:
    app: benchmark-grpc-java-armeria
    component: grpc-server
    benchmark: "true"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: benchmark-grpc-java-armeria
  template:
    metadata:
      labels:
        app: benchmark-grpc-java-armeria
        component: grpc-server
        benchmark: "true"
    spec:
      containers:
        - name: grpc-java-armeria
          image: benchmark/grpc-java-armeria:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 50051
              name: grpc
              protocol: TCP
          envFrom:
            - configMapRef:
                name: benchmark-grpc-java-armeria-config
            - secretRef:
                name: benchmark-secrets
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "256Mi"
          livenessProbe:
            grpc:
              port: 50051
            initialDelaySeconds: 30
            periodSeconds: 20
          readinessProbe:
            grpc:
              port: 50051
            initialDelaySeconds: 10
            periodSeconds: 10
      restartPolicy: Always
      terminationGracePeriodSeconds: 30
