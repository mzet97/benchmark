apiVersion: v1
kind: Service
metadata:
  name: benchmark-grpc-java-armeria
  labels:
    app: benchmark-grpc-java-armeria
    component: grpc-server
    benchmark: "true"
spec:
  type: ClusterIP
  selector:
    app: benchmark-grpc-java-armeria
  ports:
    - port: 50051
      targetPort: 50051
      protocol: TCP
      name: grpc
