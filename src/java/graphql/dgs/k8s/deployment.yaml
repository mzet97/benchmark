apiVersion: apps/v1
kind: Deployment
metadata:
  name: java-dgs-graphql
  labels:
    app: java-dgs-graphql
    benchmark: "true"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: java-dgs-graphql
  template:
    metadata:
      labels:
        app: java-dgs-graphql
        benchmark: "true"
    spec:
      containers:
        - name: java-dgs-graphql
          image: benchmark/java-dgs-graphql:latest
          ports:
            - containerPort: 8080
              name: http
          envFrom:
            - configMapRef:
                name: java-dgs-graphql-config
            - secretRef:
                name: benchmark-secrets
          resources:
            requests:
              cpu: "250m"
              memory: "512Mi"
            limits:
              cpu: "1000m"
              memory: "1Gi"
          readinessProbe:
            httpGet:
              path: /actuator/health
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /actuator/health
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 10
