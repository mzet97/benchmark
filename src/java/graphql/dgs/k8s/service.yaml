apiVersion: v1
kind: Service
metadata:
  name: java-dgs-graphql
  labels:
    app: java-dgs-graphql
    benchmark: "true"
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: java-dgs-graphql
