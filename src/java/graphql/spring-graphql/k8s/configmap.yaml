apiVersion: v1
kind: ConfigMap
metadata:
  name: spring-graphql-config
  labels:
    app: spring-graphql
    benchmark: "true"
data:
  DB_HOST: "postgresql.benchmark.svc.cluster.local"
  DB_PORT: "5432"
  DB_NAME: "benchmark"
  DB_USER: "benchmark"
  REDIS_HOST: "redis.benchmark.svc.cluster.local"
  REDIS_PORT: "6379"
