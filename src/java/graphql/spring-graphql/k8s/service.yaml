apiVersion: v1
kind: Service
metadata:
  name: spring-graphql
  labels:
    app: spring-graphql
    benchmark: "true"
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: spring-graphql
