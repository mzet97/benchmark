apiVersion: v1
kind: Service
metadata:
  name: java-quarkus
  namespace: benchmark
  labels:
    app: java-quarkus
    tier: api
spec:
  type: ClusterIP
  selector:
    app: java-quarkus
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  sessionAffinity: None
