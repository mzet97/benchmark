apiVersion: v1
kind: Service
metadata:
  name: csharp-fastendpoints
  namespace: benchmark
  labels:
    app: csharp-fastendpoints
  annotations:
    service.beta.kubernetes.io/aws-load-balancer-type: "nlb"
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: health
  selector:
    app: csharp-fastendpoints
---
apiVersion: v1
kind: Service
metadata:
  name: csharp-fastendpoints-headless
  namespace: benchmark
  labels:
    app: csharp-fastendpoints
spec:
  type: ClusterIP
  clusterIP: None
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: csharp-fastendpoints
