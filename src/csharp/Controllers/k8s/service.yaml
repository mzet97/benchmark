apiVersion: v1
kind: Service
metadata:
  name: csharp-controllers
  namespace: benchmark
  labels:
    app: csharp-controllers
  annotations:
    service.beta.kubernetes.io/aws-load-balancer-type: "nlb"
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: health
  selector:
    app: csharp-controllers
---
apiVersion: v1
kind: Service
metadata:
  name: csharp-controllers-headless
  namespace: benchmark
  labels:
    app: csharp-controllers
spec:
  type: ClusterIP
  clusterIP: None
  ports:
  - port: 8080
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: csharp-controllers
