apiVersion: apps/v1
kind: Deployment
metadata:
  name: graphql-entitygraphql
  labels:
    app: graphql-entitygraphql
spec:
  replicas: 1
  selector:
    matchLabels:
      app: graphql-entitygraphql
  template:
    metadata:
      labels:
        app: graphql-entitygraphql
    spec:
      containers:
        - name: graphql-entitygraphql
          image: graphql-entitygraphql:latest
          ports:
            - containerPort: 3000
          envFrom:
            - configMapRef:
                name: graphql-entitygraphql-config
          resources:
            requests:
              cpu: 100m
              memory: 128Mi
            limits:
              cpu: 500m
              memory: 512Mi
          readinessProbe:
            httpGet:
              path: /health
              port: 3000
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: /health
              port: 3000
            initialDelaySeconds: 10
            periodSeconds: 30
