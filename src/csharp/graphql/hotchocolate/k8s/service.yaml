apiVersion: v1
kind: Service
metadata:
  name: graphql-hotchocolate
spec:
  selector:
    app: graphql-hotchocolate
  ports:
    - protocol: TCP
      port: 3000
      targetPort: 3000
  type: ClusterIP
