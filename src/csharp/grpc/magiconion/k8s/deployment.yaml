apiVersion: apps/v1
kind: Deployment
metadata:
  name: csharp-magiconion
  labels:
    app: csharp-magiconion
    benchmark: "true"
spec:
  replicas: 1
  selector:
    matchLabels:
      app: csharp-magiconion
  template:
    metadata:
      labels:
        app: csharp-magiconion
        benchmark: "true"
    spec:
      containers:
        - name: csharp-magiconion
          image: benchmark/csharp-magiconion:latest
          ports:
            - containerPort: 50051
              name: grpc
              protocol: TCP
          envFrom:
            - configMapRef:
                name: csharp-magiconion-config
            - secretRef:
                name: benchmark-secrets
          resources:
            requests:
              cpu: "1"
              memory: "512Mi"
            limits:
              cpu: "1"
              memory: "512Mi"
          readinessProbe:
            grpc:
              port: 50051
            initialDelaySeconds: 5
            periodSeconds: 10
          livenessProbe:
            grpc:
              port: 50051
            initialDelaySeconds: 10
            periodSeconds: 30
