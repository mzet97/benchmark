apiVersion: v1
kind: Service
metadata:
  name: deno-graphql-yoga
  labels:
    app: deno-graphql-yoga
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: deno-graphql-yoga
---
apiVersion: v1
kind: Service
metadata:
  name: deno-graphql-yoga-headless
  labels:
    app: deno-graphql-yoga
spec:
  type: ClusterIP
  clusterIP: None
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: deno-graphql-yoga
