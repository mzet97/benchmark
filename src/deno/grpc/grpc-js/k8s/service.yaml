apiVersion: v1
kind: Service
metadata:
  name: benchmark-deno-grpc-js
  labels:
    app: benchmark-deno-grpc-js
spec:
  type: ClusterIP
  ports:
    - port: 50051
      targetPort: 50051
      protocol: TCP
      name: grpc
  selector:
    app: benchmark-deno-grpc-js
