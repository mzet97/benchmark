apiVersion: v1
kind: Service
metadata:
  name: benchmark-deno-connectrpc
  labels:
    app: benchmark-deno-connectrpc
spec:
  type: ClusterIP
  ports:
    - port: 50051
      targetPort: 50051
      protocol: TCP
      name: grpc
  selector:
    app: benchmark-deno-connectrpc
