apiVersion: v1
kind: Service
metadata:
  name: deno-hono
  labels:
    app: deno-hono
spec:
  type: ClusterIP
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: deno-hono
---
apiVersion: v1
kind: Service
metadata:
  name: deno-hono-headless
  labels:
    app: deno-hono
spec:
  type: ClusterIP
  clusterIP: None
  ports:
  - port: 3000
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: deno-hono
