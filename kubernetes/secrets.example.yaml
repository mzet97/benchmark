# secrets.example.yaml
# This is an EXAMPLE file with placeholder values.
# DO NOT put real credentials here.
#
# To create the actual secret, run:
#   kubectl create secret generic benchmark-secrets \
#     --namespace benchmark \
#     --from-literal=database-url="postgresql://app:<PASSWORD>@spsql.home.arpa:5432/benchmark_api" \
#     --from-literal=redis-url="redis://:<PASSWORD>@redis.home.arpa:30379"
#
# Or use Sealed Secrets / External Secrets Operator.
# See docs/SECURITY_REMEDIATION.md for full instructions.

apiVersion: v1
kind: Secret
metadata:
  name: benchmark-secrets
  namespace: benchmark
type: Opaque
stringData:
  database-url: "postgresql://app:CHANGE_ME@spsql.home.arpa:5432/benchmark_api"
  redis-url: "redis://:CHANGE_ME@redis.home.arpa:30379"
