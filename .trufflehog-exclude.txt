# TruffleHog exclusion patterns
# One pattern per line

# Example/template values
changeme
example
your_password_here
password_here
secret_here

# Test fixtures
test_password
mock_secret
fake_token

# NOTE: Markdown is deliberately NOT excluded. The 2026-07 leak had credentials
# in ~29 README/summary files; excluding *.md is what let them through.

# Dependencies
node_modules/
vendor/
target/
.pip-cache/
